export const {
  PORT,
  CLIENT_URL,
  GOOGLE_CLIENT_ID,
  EMAIL_SERVER_API_TOKEN,
  EMAIL_CREDENTIALS_USERNAME,
  EMAIL_CREDENTIALS_PASSWORD,
  EMAIL_FROM,
  AWS_CONFIG_ACCESS_KEY_ID,
  AWS_CONFIG_SECRET_ACCESS_KEY,
  AWS_S3_BUCKET_NAME,
  AWS_S3_BUCKET_REGION,
  AWS_EVENTBRIDGE_API_KEY,
  JWT_KEY,
  MONGODB_URI,
  PAYPAL_API_URL,
  PAYPAL_CLIENT_ID,
  PAYPAL_APP_SECRET,
  PAYPAL_WEBHOOK_ID,
} = process.env as { [key: string]: string };
